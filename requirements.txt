# JagDash dependencies
# Install: python -m pip install -r requirements.txt

# --- FastAPI stack ---
fastapi
uvicorn[standard]   # ASGI server
jinja2              # HTML templating
python-dotenv       # reads .env file
python-multipart    # required for Form() parsing in FastAPI
itsdangerous        # required by Starlette SessionMiddleware

# --- Authentication ---
bcrypt              # password hashing for login

# --- Data & finance ---
pandas
numpy
yfinance
pyyaml

# --- News & HTTP ---
requests            # used by news_scanner

# --- charting --- this is a NiteTrader dependency, not JagDash
#curl -L -o static/lightweight-charts.standalone.production.js \
#  https://unpkg.com/lightweight-charts/dist/lightweight-charts.standalone.production.js