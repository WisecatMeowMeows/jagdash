This is intended to be a generic dashboard application, extensible using plugins, and using a browser for the UI.  

Python is the base, HTML/CSS/JS for the front-end.  It is intended that it can be run on a server so it can be accesible from anywhere.

Early versions are intended for a single user, highly customizable to their preferences.

Future versions may be multi-user.
